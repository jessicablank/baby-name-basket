// Copy this file to `config.js` and fill in your deployment URL.
// `config.js` is gitignored so your URL stays out of the repo.
window.APP_CONFIG = {
  SHEETS_WEB_APP_URL: "https://script.google.com/macros/s/XXXXXXXXXXXX/exec",
  SHEETS_TOKEN: "some-long-random-string",
};
